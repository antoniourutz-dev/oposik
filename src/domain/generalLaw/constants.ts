/**
 * Masa mínima de preguntas para considerar un bloque en recomendaciones y narrativa
 * (evita “entrenar” bloques casi vacíos).
 */
export const MIN_QUESTIONS_FOR_BLOCK_TERRITORY = 8;

/** Default editorial al publicar una ley (puede sobrescribirse por fila en BD). */
export const DEFAULT_MIN_QUESTIONS_TO_PUBLISH_LAW = 30;
